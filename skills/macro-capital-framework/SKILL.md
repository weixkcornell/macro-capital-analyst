---
name: macro-capital-framework
description: '观澜宏观与资本市场分析工艺：九层能力取机制、六步内化组织行文、两道闸门把关数字与可执行性、五道起步门禁验收。Triggers on "宏观研判", "A股策略", "周期定位", "风格配置", "资产配置", "因子归因", "市场观点看板", "净α测算", "预期收益"'
version: 2.1.0
user-invocable: true
argument-hint: "[写/审] 中国宏观与 A 股 [主题]"
license: MIT
metadata:
  short-description: 观澜宏观与 A 股分析的完整工艺规范
---

# 宏观与资本市场分析工艺（macro-capital-framework）

> 一句话总纲：结论先行、层层递进、逐条可核验；无测算不下结论，净 α ≤ 0 不发布。

## §0 模板引用（先读）

- 权威附件：`references/checklist-template.md`（交付前逐项打勾）。
- 输出骨架：`output-templates/a-share-outlook.json` / `cycle-positioning.json`。
- 门禁细则：`quality-policies/a-share-outlook-quality.json` / `cycle-positioning-quality.json`。
- 方法包：`method-packs/six-step-internalization.json`（六步内化流程）/ `nine-layer-framework.json`（九层能力框架）/ `dual-gates.json`（两道横切闸门）——三者的权威步骤定义。
- 三方对撞参考实现：`references/scripts/i1_collision_check.py --md <report.md> --pool <data.json> ... --out <i1-report.json>`。

本文件只规定执行规则，不重复上述实体内容。

## §1 行文逻辑（怎么写）

1. **结论先行**：开篇给一句话判断，再展开证据；每个判断挂一个量化锚。
2. **层层递进不罗列**：观点之间呈现推导关系，每层结尾给「本层结论」并自然引出下一层；禁止「观点 A；观点 B；观点 C」式的罗列。
3. **六步组织**：市场观点类产出按「钱从哪来 → 钱变利润 → 价格装多少预期 → 结构定价自洽 → 周期定位风险归属 → 错了会怎样」递进。
4. **方法论内化**：正文不得出现书名、作者名、「某书认为」等书目痕迹；方法只用于判断，不作为可见内容。文末「方法论说明」只描述本人分析流程。
5. **两个强制独立章节**：市场观点类产出必须含「上期判断回顾」（逐条标注已验证/未验证/证伪 + 累计命中率）与「方法可靠性声明」（显式声明局限）。

## §2 数据要求（怎么用数）

1. **口径锚定**：宏观数字先读对——名义/实际、同比/环比、结构拆解、基数与季节性；锚定官方口径。
2. **四级数据标注**：每个非引用数字必须落到一级——`测算`（模型算，给参数）/ `估算`（量级推断）/ `研判推断`（分析性判断）/ `待补`（进观察清单）。
3. **来源可点击**：引用数据/观点挂来源链接，严禁编造；券商观点只作对照组，给出「认同/保留/反对及理由」。
4. **一手自算**：行情用公开 K 线自算四档分位、波动、回撤、相关矩阵、拥挤度、风险贡献分解；估值分位若用外部口径必须标注来源。
5. **海外锚重标定**：海外实证数值只作数量级参考，A 股须重新标定，严禁当默认判据。
6. **铁律：编数字比不回答更严重**；数据不可得 → 如实标「待补」进观察清单。
7. **量纲与口径交叉校验**：外部行情/金融数据字段用于绝对额结论前，必须先与权威口径做比例校验；同一字段比例漂移 >5% 时，绝对额一律改用交易所/官方口径，外部字段只用于比值、分位和方向判断。

## §3 质量门禁（怎么过关）

1. **两道横切闸门**（硬门，所有结论强制生效）：闸门 A 统计可信（t 阈值分档 / 样本量 / 夏普 50% 折 / 肥尾禁用正态外推）；闸门 B 摩擦可执行（毛 α − 摩擦 = 净 α，净 α ≤ 0 不发布）。
2. **五道起步门禁**（见 `quality-policies/*.json`）：禁例 token 0 命中、数字逐 token 一致、渲染 0 溢出 + 对比度 AA、排版审计 0 问题、章节大纲对照输出模板。
3. **摘要—正文—底座三方对撞**：摘要中的每条概括必须在正文找到对应论据、并在数据底座找到同口径证据；三方不一致时不得发布，先回底座查错。参考实现：`references/scripts/i1_collision_check.py --md <report.md> --pool <data.json> ... --out <i1-report.json>`。
4. **合规红线**：对外匿名呈现（领域·首字母）；文末固定「不构成投资建议」；未发布官方数据显式标注「尚未发布」；渲染产物发布为公网外链前，先确认内容不涉密、可公开。
5. **交付前逐项过** `references/checklist-template.md`。
